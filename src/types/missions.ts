/**
 * All the possible statuses for challenges, goals, and projects.
 */
export enum StatusTypes {
  CREATED = 'created',
  RUNNING = 'running',
  CANCELED = 'canceled',
  COMPLETED = 'completed',
}
