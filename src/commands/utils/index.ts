import { deployCommands } from './deploy'
import { executeTopLevelCommand } from './execute-top-level-command'
import { printGenericError } from './print-generic-error'

export const CommandUtils = {
  deployCommands,
  executeTopLevelCommand,
  printGenericError
}
